using sc = System.Console; //same as "import" used in other languages which imports the whole the file,
              //except in C# we are only linking to a file

namespace HelloWorld //name variables using the pascal casing convnetion = the first letter of every word capitalized
{ // all braces are their own line
  class Program
  {
    static void Main(string[] args)
    {
        sc.WriteLine("Hello World!");
    }
  }
}
